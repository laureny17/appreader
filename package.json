{
  "name": "appreader",
  "version": "1.0.0",
  "description": "A demo app implementing the ApplicationAssignments concept with Gemini LLM integration",
  "main": "dist/application-assignments-tests.js",
  "scripts": {
    "build": "tsc",
    "test": "mocha -r ts-node/register tests/**/*.ts",
    "manual": "mocha -r ts-node/register tests/application-assignments-tests.ts --grep 'Manual'",
    "llm": "mocha -r ts-node/register tests/application-assignments-tests.ts --grep 'LLM'",
    "mixed": "mocha -r ts-node/register tests/application-assignments-tests.ts --grep 'Mixed'"
  },
  "keywords": [
    "gemini",
    "ai",
    "schedule",
    "student",
    "demo",
    "typescript"
  ],
  "author": "Student Demo",
  "license": "MIT",
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.0.0",
    "mocha": "^11.7.4",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
